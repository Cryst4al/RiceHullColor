# Example data

`metadata_template.csv` only demonstrates the column schema. The two rows are synthetic and must not be used to reproduce biological conclusions.

No experimental photographs or unpublished material identifiers are included in the public repository. To test image analysis, run `python -m unittest`: the test suite creates synthetic hull-like ellipses in a temporary folder.

