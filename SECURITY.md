# Security and data privacy

Report security problems privately to the repository maintainer rather than opening a public issue. RiceHullColor processes local files and does not upload data. Users remain responsible for checking that photographs, labels and phenotype tables are authorised for publication. The default `.gitignore` excludes experimental image folders and RAW/XMP files.

