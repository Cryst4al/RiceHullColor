# 算法与统计方法

## 图像测量

软件将输入解释为 sRGB IEC61966-2.1。像素先做 sRGB 逆伽马变换并转换到 XYZ D65，再用 Bradford 矩阵适配至 D50，最后计算 CIELAB D50。

默认颖壳组织条件为 `a* < -5`、`b* > 5`、`15 <= L* <= 90`。掩膜以 2 倍降采样、2 次膨胀连接组织片段，然后执行 8 邻域连通域分析。小连通域和小于最大候选面积 10% 的碎片被滤除；未设置 `expected_count` 时，保留其余连通域（当前项目最多 10 个），所以 6–10 粒等实际数量均可输出。若给出预期粒数，它用于选择面积最大的候选与质控提示，不会伪造缺失 ROI。

每个连通域仅测量其长轴中央 70%，以减少颖尖、芒、护颖和边缘背景的影响。逐粒记录像素数与 L*/a*/b* 均值、中位数和标准差。逐粒平均 Lab 再反算为 8 位 sRGB；灰度为：

```text
Gray = 0.299 R + 0.587 G + 0.114 B
```

ImageJ ROI.zip 使用凸包轮廓，适合复核和交换；正式解释前应查看 QC 叠加图。自动阈值不是普适的颜色标定，换相机、光源、背景或生育期后必须重新验证阈值。

## 汇总层级

- 逐粒：单个 ROI，是技术亚样本；
- 逐图像：按有效像素数加权汇总 ROI；
- 逐材料日期：同一材料、同一天的图像均值等权平均；
- 逐材料：各日期/批次的材料日期均值等权平均。

生物学推断以材料为统计单位。不能把同一材料的 10 粒或 3 张图当成 30 个独立材料。

## 籼粳关联分析

单变量模型为 `trait ~ Date + Group`，报告粳稻减籼稻的校正差、95% CI、P 值、偏 R² 和 Hedges' g；L*、a*、b*、Gray 的 P 值用 Benjamini–Hochberg 方法控制 FDR。

多变量颜色空间使用标准化 L*/a*/b* 的日期校正置换检验，置换限制在日期内部，并报告 pseudo-F、置换 P 和偏 R²。PCA 只用于可视化分布与重叠，不作显著性检验，也不需要改成“火山图”。

分类能力用 L2 正则化 Logistic 回归与重复分层交叉验证评估，报告 AUC、平衡准确率、灵敏度和特异度。日期校正在每个训练折内拟合，避免把测试集信息泄漏到模型。若不同日期材料构成严重不平衡，还应补做按日期留一法；若某个日期只有一个籼粳组，则日期效应和籼粳效应无法完全区分。

这些结果可以说明“颖壳颜色表型与籼粳属性存在多强的统计关联及预测信息”，不能单凭表型证明籼粳分化的遗传机制。灰度与 L* 通常高度相关，应视为辅助展示而不是独立的第二条证据。
