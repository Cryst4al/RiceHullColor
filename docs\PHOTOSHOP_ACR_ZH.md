# Adobe Camera Raw / Photoshop 前处理规范

本规范适用于 Photoshop 2026（27.x）与 Adobe Camera Raw 18.x。不同小版本中的“保存图像”和“转换并存储”功能相同。

## RAW 与颜色校正

1. 原始 ARW 存入 `00_RAW原始备份`，工作副本存入 `01_RAW工作副本`；参数保存为同名 `.xmp` 附属文件。
2. 在 Camera Raw 中“重置为 Camera Raw 默认值”，不要点“自动”。
3. 配置文件选择“Adobe 中性”；不使用鲜艳、风景或创意配置文件。
4. 曝光、对比度、高光、阴影、白色色阶、黑色色阶、自然饱和度、饱和度、纹理、清晰度、去朦胧、暗角、颗粒均从 0 开始。
5. 用比色卡中等亮度灰块中央校正白平衡，避开边缘、反光、阴影、污点和过曝白块，并记录色温、色调和曝光修正。
6. 开启“移除色差”和“使用配置文件校正”；不使用自动几何校正，只允许整体旋转。
7. 锐化数量和明亮度降噪设为 0；不使用 AI 降噪和超级分辨率。
8. 检查颖壳不过曝、无大面积欠曝、灰阶逐级可辨；必要时只能调整全局曝光。

## CAL 输出参数

- 色彩空间：sRGB IEC61966-2.1；
- 位深：16 位/通道；
- 尺寸：相机原始像素，不调整大小；
- 分辨率：300 ppi（只影响打印尺寸）；
- 输出锐化：无；
- TIFF 压缩：ZIP 或 LZW；
- 文件名：`材料-HULL-日期-I01_CAL.tif`。

CAL 必须保留全部样品、比色卡、标尺、标签和原始背景，不允许修复污点、替换背景或局部调色。确认“编辑 → 转换为配置文件”中的源空间为 sRGB；不要使用“指定配置文件”重新解释 RGB 数值。

## ROI 分析图

在 CAL 副本上仅做裁切，保留全部待测颖壳和适量背景；关闭生成式扩展与内容识别，不重新采样、不缩放。保存为 16 位、嵌入 sRGB 配置文件、无图层的 `*_ROI.tif`。随后交给 RiceHullColor：

```bash
ricehullcolor batch 项目文件夹 --config hull_color_global_v1.yml
```

自动分割后必须检查 `04_QC预览图`。粘连、强反光、阴影、病斑或错误背景造成的 ROI 应在 Fiji/ImageJ 中人工复核。

## 禁止操作

正式 CAL/ROI 不得使用自动色调、自动颜色、局部蒙版调色、饱和度增强、局部曲线、修复/仿制、内容识别、生成式填充、液化、透视变形、图像重采样或 JPEG 反复保存。

